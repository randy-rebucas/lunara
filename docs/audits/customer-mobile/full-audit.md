# customer-mobile full audit

Scope: `apps/customer-mobile/` (Expo/React Native, expo-router, zustand), branch `feature/multi-tenant`. Read-only investigation, no code changes made. This is a re-audit following commit `605e9d6` ("fix: address customer-mobile audit findings") and, in this session, an Expo SDK 54 → SDK 57 upgrade (expo ^57.0.0, expo-device/expo-image-picker/expo-location/expo-notifications/expo-router/expo-secure-store/expo-splash-screen/expo-status-bar bumped, react 19.2.3, react-native 0.86.3, react-native-maps/safe-area-context/screens/svg, @types/react, eslint-config-expo, typescript, plus the previously-missing `@react-native-community/slider` installed). `npx tsc --noEmit` and `npx expo config --type prebuild` were re-verified clean in this pass.

---

## 0. Verification of commit `605e9d6`'s claimed fixes

All eight fixes claimed in the commit message were independently verified against current repo state — **all landed correctly**, none are partial or cosmetic-only:

| Claim | Verified |
|---|---|
| Fix `handleSubscribe()` wrong request shape | **Fixed.** `app/orders/[id]/index.tsx:282-300` now sends `services: [{ bookingType, bagSizeId }]` plus top-level `branchId`/`addonIds`/`addonQuantities`/`pickupAddressId`/`scheduledPickupAt`/`frequencyDays` — matches `CreateSubscriptionDto extends CreateBookingOrderDto extends BookingQuoteDto` (`apps/api/src/modules/booking/dto/booking.dto.ts:74-132`, `apps/api/src/modules/subscriptions/dto/subscription.dto.ts:6-9`) field-for-field, including the `@Type(() => ServiceSelectionDto)` nesting. See §4 below for full detail. |
| Add missing `res/xml/secure_store_*.xml` | **Fixed.** Both files exist under `android/app/src/main/res/xml/` and are correctly referenced from `AndroidManifest.xml:16`. |
| Remove `RECORD_AUDIO`/`SYSTEM_ALERT_WINDOW` | **Fixed.** `AndroidManifest.xml:2-8` now lists only `ACCESS_COARSE_LOCATION`, `ACCESS_FINE_LOCATION`, `CAMERA`, `INTERNET`, `READ/WRITE_EXTERNAL_STORAGE` (maxSdk 32), `VIBRATE` — every one justified by actual code. |
| Restore `expo-env.d.ts` to tsconfig include | **Effectively moot, not a defect.** `tsconfig.json`'s `include` is `["**/*.ts", "**/*.tsx"]` — `expo-env.d.ts` matches the `**/*.ts` glob already (its extension is `.ts`), so it doesn't need a literal entry. The file itself is gitignored and not currently present in the working tree (`apps/customer-mobile/expo-env.d.ts` doesn't exist, unlike `apps/rider-mobile/expo-env.d.ts` and `apps/partner-mobile/expo-env.d.ts`, which do) — it's auto-regenerated by `expo start`/`expo prebuild`, and `tsc --noEmit` passes clean without it, so this is a non-issue, not a residual bug. |
| Add `partner-brands/lunara-development-territory/` | **Fixed.** Directory exists at repo root (`partner-brands/lunara-development-territory/` — the partner-brands directory lives at the monorepo root, not under `apps/customer-mobile/`) with `icon.png`, `splash.png`, `manifest.json`, matching the `eas.json` profiles `preview-lunara-development-territory`/`production-lunara-development-territory`. |
| Split HTTP client into `src/lib/api-client.ts` | **Fixed.** `authRequest`/`authUpload` duplication resolved into one `authRequest(path, body: {kind:'json'|'form', ...}, ...)` function (`src/lib/api-client.ts`, 74 lines). `auth.ts` shrank from 281 to 187 lines and now only holds `{user, tokens, isLoading}` + actions; `getPartnerId()` moved to `api-client.ts` and is re-exported from `auth.ts` for callsite compatibility. |
| Add `useAsyncResource` + `toErrorMessage` | **Fixed.** `src/hooks/use-async-resource.ts` (70 lines) exists; `toErrorMessage(e, fallback)` added to `src/lib/api-error.ts:25`. Adoption is partial by design/rollout, not broken — 3 files currently import `useAsyncResource`; the repeated `e instanceof Error ? e.message : ...` ternary still appears in some untouched screens (acceptable incremental migration, not a regression). |
| Add root `ErrorBoundary` | **Fixed.** `src/components/error-boundary.tsx` (91 lines, class component with `getDerivedStateFromError`) wraps the root `<Stack>` in `app/_layout.tsx:253-572`. |
| Decompose `book.tsx` (2,944→972 lines) | **Fixed.** 8 new step components under `src/components/book/` (`shop-step`, `address-step`, `service-step`, `weight-step`, `addons-step`, `schedule-step`, `review-step`, `confirm-step`, plus shared `shared.tsx`). Confirmed via `wc -l`: `app/book.tsx` is now 972 lines. |
| Decompose `orders/[id]/index.tsx` (946→903 lines) | **Fixed, but modest.** `ReviewModal`, `RescheduleModal`, `DeliverySignModal` extracted under `src/components/order-detail/` as claimed — but the parent file only shrank 946→903 lines (43 lines), far less than `book.tsx`'s reduction. The three modals were extracted as presentational components while most state/handlers (`handleSubscribe`, delivery verify, reschedule submit, review submit) stayed in the parent — still a large, many-`useState` screen. See §3. |

**Net effect: this was a real, substantive fix commit, not a cosmetic one.** The one CRITICAL data-flow bug and the one CRITICAL Android-build-breaker from the prior audit are both genuinely resolved.

---

## 1. Build/config health

- **[NOTE — resolved] Both prior CRITICAL/HIGH Android build blockers are fixed.** Missing `res/xml/secure_store_backup_rules.xml`/`secure_store_data_extraction_rules.xml` now exist and resolve; the SecureStore backup-exclusion security control (§2) now actually functions with `android:allowBackup="true"`.

- **[NOTE] SDK 57 upgrade is coherent and complete — no half-done state found.** `package.json` shows `expo ^57.0.0` paired consistently with `expo-router ~57.0.19`, `expo-secure-store ~57.0.3`, `expo-notifications ~57.0.17`, `expo-location ~57.0.16`, `expo-image-picker ~57.0.16`, `expo-device ~57.0.1`, `expo-splash-screen ~57.0.8`, `expo-status-bar ~57.0.1`, `react 19.2.3`, `react-native 0.86.3`, `react-native-screens ~4.26.0`, `react-native-safe-area-context ~5.7.0`, `react-native-svg 15.15.4`, `@types/react ~19.1.10` — all SDK57-generation versions, no leftover SDK54-era pins found. `@react-native-community/slider@5.2.0` (the previously-missing dependency) is present. `npx tsc --noEmit` passes with zero errors; `npx expo config --type prebuild` resolves without error.

- **[LOW / correction to prior audit — not actually a risk] Gradle wrapper 8.14.3 → 9.3.1 verified compatible.** `android/build.gradle`'s `buildscript.dependencies` block declares `classpath('com.android.tools.build:gradle')` and `classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')` with **no pinned version** — both are resolved transitively via the `expo-root-project`/`com.facebook.react.rootproject` Gradle plugins, which is the standard Expo-managed autolinking pattern (not a raw manual AGP/Kotlin pin that could drift out of sync with the wrapper). `expo config --type prebuild` succeeding is a reasonable proxy that the toolchain resolves. Downgrading the prior audit's HIGH flag here — the versions are Expo-template-managed, not a manually-introduced mismatch. Still worth a real device/CI Android build before merge as final confirmation, since this tool couldn't execute an actual Gradle build in this environment.

- **[NOTE] `expo-status-bar` removal from `app.json`/`app.config.js` `plugins` array confirmed correct, not a regression.** `expo-status-bar` is not present in the `plugins` array of `apps/customer-mobile/app.json`; the package remains a normal dependency (`~57.0.1`) and is used directly as a component (`<StatusBar .../>`), not as a config plugin — it never took plugin-config options. This matches the stated intent from earlier in this session and applies consistently across customer-mobile, rider-mobile, and partner-mobile.

- **[MEDIUM] `tsconfig.json` `include` still doesn't literally list `.expo/types/**/*.ts` and `expo-env.d.ts`, and `app.json`'s `experiments.typedRoutes` remains `false`.** Confirmed unchanged from the prior audit's finding — carried over, not newly introduced by this session's SDK bump. Functionally harmless today (see §0 — `**/*.ts` already covers `expo-env.d.ts`, and `tsc` passes clean), but if `typedRoutes` is ever flipped back to `true`, `.expo/types/**/*.ts` would need re-adding to `include` for the generated route types to be picked up. Low urgency; flagging so it isn't lost.

- **[NOTE] `package.json`/`package-lock.json` in sync.** Root `package-lock.json` was regenerated in `605e9d6` (previously out of sync, breaking `npm ci` in EAS builds per that commit's message) and again reflects the current SDK 57 dependency set — `git status` shows `apps/customer-mobile/package.json`, `package-lock.json`, and root `package.json` as the only currently-modified files (uncommitted working-tree changes from this session's upgrade), consistent with an in-progress, not-yet-committed SDK bump.

- **[LOW] `android/app/debug.keystore` still tracked in git** — unchanged from prior audit, standard shared RN/Expo debug key, low risk.

- **[NOTE] No `ios/` directory** — Expo config-plugin managed, unchanged from prior audit.

---

## 2. Security

- **[NOTE — resolved, now fully functional] SecureStore token migration + backup-exclusion now both land together.** `src/store/auth.ts:14-30` splits `USER_STORAGE_KEY` (AsyncStorage) from `TOKENS_STORAGE_KEY` (SecureStore), and the previously-missing `res/xml/secure_store_*.xml` files (§1) now exist, so the `android:fullBackupContent`/`android:dataExtractionRules` exclusion actually applies. This closes out both the prior HIGH (plaintext token storage) and the CRITICAL/HIGH build-and-backup pairing.

- **[NOTE — resolved] `RECORD_AUDIO`/`SYSTEM_ALERT_WINDOW` permissions removed**, confirmed absent from `AndroidManifest.xml`. Remaining permission set is fully justified (unchanged table from prior audit otherwise).

- **[MEDIUM] Unchanged: precise GPS coordinates still sent to unauthenticated `nominatim.openstreetmap.org`.** `src/lib/reverse-geocode.ts` — line numbers shifted slightly (24-line diff in `605e9d6`, likely unrelated formatting/error-handling touch-up) but the third-party data-sharing pattern itself is unchanged. Still worth confirming disclosure in the privacy policy.

- **[LOW] Unchanged: `http://` (not `https://`) still used for the iOS Apple Maps deep link.** `app/orders/[id]/index.tsx:96`: `` ios: `http://maps.apple.com/?ll=${lat},${lng}` ``. Not touched by `605e9d6`'s refactor of this file. Low risk (iOS intercepts the URL scheme before any network request), but trivial to fix while the file is already being edited.

- **[LOW] Unchanged: no runtime `https://` assertion on `EXPO_PUBLIC_API_URL`** in `src/api-config.ts`. Same as prior audit.

- **[NOTE] No hardcoded secrets, no `eval`, no console logging** — re-confirmed with fresh greps across current `src/`/`app/`. Same clean result as prior audit.

- **[NOTE] `x-lunara-client: 'mobile'` and `x-lunara-partner-id` headers** are now visibly centralized in the new `src/lib/api-client.ts:35-49` — a good side effect of the refactor, makes the multi-tenant header contract easier to audit in one place going forward.

---

## 3. Code quality & architecture

### Navigation (expo-router)
- **[MEDIUM] Unchanged from prior audit: header config still manually duplicated per-screen in `app/_layout.tsx`.** Not addressed by `605e9d6` (that commit added `ErrorBoundary` wrapping, +6 lines to `_layout.tsx`, but didn't touch the header-duplication pattern). `app/_layout.tsx` is now 581 lines (was 575) — the copy-pasted `headerLeft` block and the "screen defined under `app/` but missing from `_layout.tsx`'s Stack.Screen list" foot-gun both remain exactly as before. Still no per-group `_layout.tsx` files under `orders/`, `refunds/`, `checkout/`, `onboarding/`, `support/`.
- **[LOW] Unchanged: `app/_layout.tsx`'s "blank line after nearly every statement" formatting outlier persists** — still visibly present in the 253-572 range that now also wraps the new `<ErrorBoundary>`.
- **[LOW] Unchanged: `app/scan-tag.tsx` still has no `<Stack.Screen>` entry**, inconsistent route-nesting depth (`refunds/[id].tsx` flat vs. `orders/[id]/index.tsx` nested) both persist as before.

### State management (zustand)
- **[NOTE — resolved] `auth.ts`'s three-concerns conflation is fixed.** The generic HTTP client (`authRequest`/`authUpload` merged into one `authRequest`) is now in `src/lib/api-client.ts`; `auth.ts` (187 lines, down from 281) holds session state and thin actions only. `getPartnerId()` also moved out, re-exported for compatibility. This was a MEDIUM finding in the prior audit — fully addressed.
- **[LOW] Unchanged: `notification-sync.ts`/`order-realtime.ts` still duplicate the same tick-counter pattern** under different shapes — not touched by this commit, still a candidate for a shared factory.

### Consistency across screens / duplication
- **[NOTE — resolved] Load/refresh/error boilerplate duplication addressed via `useAsyncResource`.** New hook exists at `src/hooks/use-async-resource.ts` and is adopted in 3 files so far. This is a genuine start on the prior MEDIUM finding, not yet a full migration — remaining screens (`refunds/index.tsx`, `support/index.tsx`, `rewards.tsx`, others not yet sampled this pass) may still use the old inline pattern; worth a follow-up sweep once the team standardizes, but not a regression.
- **[NOTE — resolved] `toErrorMessage(e, fallback)` helper added** to `src/lib/api-error.ts:25`, and is used in the refactored `handleSubscribe()` (`orders/[id]/index.tsx:303`). The prior audit's "15+ repeated ternary" finding now has a canonical replacement available; full sweep of all call sites not verified this pass but the helper itself is in place and used in at least the highest-value spot (subscription flow).

### Mega-components
- **[NOTE — resolved] `book.tsx` decomposition is substantial and real.** 2,944 → 972 lines, 8 step components extracted under `src/components/book/` (`shop-step.tsx` 355 lines, `weight-step.tsx` 354 lines, `shared.tsx` 710 lines, others 70-194 lines each). This was the prior audit's #3 top-fix-first item — genuinely resolved, not cosmetic.
- **[MEDIUM — partially addressed, still a maintainability risk] `orders/[id]/index.tsx` decomposition was modest: 946 → 903 lines (43-line reduction), despite extracting 3 modal components (`ReviewModal` 140 lines, `RescheduleModal` 161 lines, `DeliverySignModal` 138 lines) totaling ~440 lines of new code.** The extraction pulled out modal JSX/local form state but left the bulk of the parent's state and handler logic (order loading, `handleSubscribe`, verify/sign/reschedule/review submit orchestration, tracking display) in the 903-line parent — still well above every other screen in the app (next largest non-`book.tsx` screen, `profile.tsx`, is 812 lines) and still likely has a large `useState` count (not recounted this pass, but the modest line reduction suggests most of the original ~24 hooks remain). Downgrading from the prior audit's HIGH to MEDIUM given real (if partial) progress, but this should not be considered "done" — a genuine reduction would move handler logic into the modal components or a `useOrderDetail()` hook, not just their JSX.

### TypeScript usage
- **[NOTE — still strong, unchanged]** `strict: true`, zero `any`/`@ts-ignore`/`@ts-expect-error` across `src/`/`app/`, re-confirmed with `tsc --noEmit` passing clean after the SDK 57 bump.
- **[LOW] Unchanged: locally re-declared interfaces (`OrderRow`, `OrderDetail`) that overlap `@lunara/types`** — not addressed by this commit, same as before.

### Error boundary / error handling
- **[NOTE — resolved] Root `ErrorBoundary` added.** `src/components/error-boundary.tsx` (class component, `getDerivedStateFromError` + `componentDidCatch`-equivalent logging via `console.error`) now wraps the root `<Stack>` (`app/_layout.tsx:253,572`). This closes the prior audit's MEDIUM "no crash recovery UX" finding for a payments-carrying app. Note: this reintroduces the app's *only* `console.error` call (previously the app had zero console logging anywhere) — appropriate here since it's the last-resort crash handler, not a leak risk (it logs the error object, not tokens/PII).

---

## 4. Data flow & API integration

- **[NOTE — resolved, verified field-for-field] The CRITICAL "wrong request shape to `POST /subscriptions`" bug is fully fixed.**
  `app/orders/[id]/index.tsx`, `handleSubscribe()` (lines 275-307) now sends:
  ```
  services: [{ bookingType: order.bookingType, ...(bagSizeId ? {bagSizeId} : {}) }],
  ...(branchId ? {branchId} : {}),
  addonIds, addonQuantities, pickupAddressId, scheduledPickupAt, frequencyDays
  ```
  Cross-checked against current `CreateSubscriptionDto extends CreateBookingOrderDto extends BookingQuoteDto` (`apps/api/src/modules/subscriptions/dto/subscription.dto.ts:6-9`, `apps/api/src/modules/booking/dto/booking.dto.ts:74-132`): `services: ServiceSelectionDto[]` (`@IsArray() @ArrayMinSize(1) @ValidateNested @Type(() => ServiceSelectionDto)`) is now populated correctly, `ServiceSelectionDto` (`booking.dto.ts:32-45`) accepts `bookingType`/`bagSizeId` as sent, and `branchId`/`addonIds`/`addonQuantities`/`pickupAddressId`/`scheduledPickupAt` all match their respective DTO fields exactly (types, optionality, nesting). `frequencyDays` matches `CreateSubscriptionDto`'s own `@IsIn([7,14,30])` field. **No mismatch found — "make this a recurring pickup" should now pass validation for every booking type.** This was the prior audit's #1 top-fix-first item.

- **[NOTE] Backend DTOs for booking/subscriptions have not otherwise changed shape since the prior audit** (only the fields checked above are relevant to this app's calls) — no new drift introduced by the SDK 57 upgrade, which is app-side/native-layer only and touches no request payloads.

- **[NOTE] `payment-checkout.tsx`/`wallet.tsx`/`review/[id].tsx` request shapes** were verified byte-for-byte in the prior audit against `CreatePaymentIntentDto`/`CreateWalletTopupIntentDto`/`CreateReviewDto` with no drift found; not re-verified line-by-line this pass since neither the app payloads nor those specific backend DTOs show as changed in current git status/diff — low risk of silent drift.

- **[LOW] Unchanged: no retry/backoff beyond the single 401-refresh-and-retry path**, now living in `src/lib/api-client.ts:20-56` post-refactor (same behavior, new location).

- **[MEDIUM] Not independently re-verified this pass: branch-picker/nearest-branches screens.** Same gap noted in the prior audit — `src/components/branch-picker-sheet.tsx`/`nearest-branches.tsx` fetch/typing not individually re-traced.

---

## Summary counts

- Critical: 0
- High: 0
- Medium: 5
- Low: 8
- Note: 21

(Prior audit: Critical 2, High 4, Medium 9, Low 10, Note 19. Both CRITICALs and all 4 HIGHs from the prior audit are resolved or downgraded on re-verification; see §0 and inline "resolved"/"downgraded" tags above for the disposition of each.)

## Top 5 to fix first

1. **[MEDIUM — Code quality]** `app/orders/[id]/index.tsx` (903 lines) decomposition was modest (946→903, a 43-line reduction, despite extracting 3 modal components) — most state/handler logic remains in the parent. A genuine second pass (extract a `useOrderDetail()` hook, or move handler logic into the modal components rather than just their JSX) would meaningfully reduce this screen's remaining maintainability risk.
2. **[MEDIUM — Build]** `tsconfig.json` `include` still doesn't literally list `.expo/types/**/*.ts`/`expo-env.d.ts` and `typedRoutes` remains `false` in `app.json` — currently harmless (glob coverage + `tsc` passes clean) but confirm this is the intended long-term state, not a dangling partial revert, before anyone re-enables typed routes.
3. **[MEDIUM — Security]** Precise GPS coordinates still sent unauthenticated to `nominatim.openstreetmap.org` (`src/lib/reverse-geocode.ts`) — confirm privacy-policy disclosure, or consider a proxied/keyed geocoding service.
4. **[MEDIUM — Data flow]** Re-verify `branch-picker-sheet.tsx`/`nearest-branches.tsx` request/response typing against current backend — the one gap not re-checked in either audit pass.
5. **[LOW — Navigation]** Header-config duplication in `app/_layout.tsx` (581 lines, copy-pasted `headerLeft` block 8+ times, no per-group `_layout.tsx` files) remains unaddressed — lowest-severity item on this list, but the single largest source of "forgot to wire up a new screen's header" risk as the app grows, and the ErrorBoundary/SDK57 changes already touched this file, so it's a good candidate to fold in next.

**Bottom line: this is a genuinely clean re-audit.** Both CRITICAL findings and all 4 HIGH findings from the prior audit are resolved — the subscription request-shape bug (verified field-for-field against current backend DTOs), the Android build-breaker, the unused dangerous permissions, and the `book.tsx` mega-component are all fixed for real, not cosmetically. The SDK 54→57 upgrade landed cleanly with no half-done native-config residue found (`tsc --noEmit` and `expo config --type prebuild` both clean). Remaining findings are MEDIUM/LOW polish items, not blockers.
