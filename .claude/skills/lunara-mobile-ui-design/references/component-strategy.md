# Component Strategy

Use:
Primitive → Basic → Composite → Domain → Screen

Create reusable components when they have meaningful reuse, state, domain meaning, complexity, or independent evolution.

Do not put API/business logic in primitives.

Do not duplicate shared Lunara UI components.
