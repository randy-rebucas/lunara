# Visual QA

Loop:
render → capture → compare → identify → fix → render again

Priority:
P1 structure
P2 typography
P3 spacing
P4 visual polish

Validate the reference viewport and other realistic mobile sizes.

Fix the largest discrepancy first.
