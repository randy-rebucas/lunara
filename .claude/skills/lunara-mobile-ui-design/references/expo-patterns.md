# Expo / React Native Patterns

Use project conventions first.

Prefer Expo/React Native primitives and capabilities for:
safe areas, keyboard behavior, Pressable, TextInput, camera, location, notifications, secure storage, network state, haptics, accessibility, and Expo Router.

Avoid web-specific assumptions and absolute positioning for normal flow.
